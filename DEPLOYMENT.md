# راهنمای پابلیش برنچ Standalone Cloudflare (۱۰۰٪ مستقل و بدون نیاز به سرور)

این برنچ (`standalone-cloudflare`) طوری ساخته شده است که **تمام APIها، داده‌های نمونه ترید، تقویم اقتصادی، سیگنال‌ها، اخبار و سیستم هشدارها را به صورت توکار (In-Browser Mock Engine)** همراه خود دارد.

### مزایای این برنچ:
1. **بدون نیاز به هیچ سرور پایتون یا هاست خارجی**
2. **بدون نیاز به ویزاکارت یا کارت اعتباری**
3. **پابلیش ۱۰۰٪ رایگان و آنی روی Cloudflare Pages**
4. **سرعت لود فوق‌العاده بالا روی شبکه جهانی CDN کلودفلر**
5. **پشتیبانی کامل از ایجاد/حذف هشدارها و شخصی‌سازی داشبورد با ماندگاری در مرورگر (localStorage)**

---

## نحوه پابلیش روی Cloudflare Pages (فقط در ۲ دقیقه)

1. وارد پنل مدیریت **[Cloudflare Dashboard](https://dash.cloudflare.com)** شوید.
2. از منوی سمت چپ به مسیر **Compute (Workers) > Workers & Pages** بروید.
3. روی دکمه **Create application** کلیک کرده و تب **Pages** را انتخاب کنید.
4. روی **Connect to Git** کلیک کنید و ریپازیتوری خود یعنی `abbasi0abolfazl/FXBrain` را انتخاب کنید.
5. در صفحه تنظیمات بیلد:
   * **Project name**: یک نام دلخواه (مثلاً `fxbrain`)
   * **Production branch**: حتماً برنچ **`standalone-cloudflare`** را انتخاب کنید.
   * **Framework preset**: گزینه **`Vite`**
   * **Build command**: `npm run build`
   * **Build output directory**: `dist`
   * *(به بخش Environment variables اصلاً نیازی نیست دست بزنید!)*
6. روی دکمه **Save and Deploy** کلیک کنید.

سایت شما در کمتر از ۳۰ ثانیه روی یک آدرس اختصاصی کلودفلر (مانند `https://fxbrain.pages.dev`) با گواهی SSL رایگان بالا می‌آید و کاملاً فعال و کاربردی خواهد بود.

---

## تست محلی در کامپیوتر خودتان

برای اجرای پروژه روی این برنچ به صورت محلی:
```bash
npm install
npm run dev
```
سایت در آدرس `http://localhost:5173` باز شده و تمام امکانات بدون نیاز به اجرای سرور پایتون کار خواهند کرد.
