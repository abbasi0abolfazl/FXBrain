# Production API URL (set in Cloudflare Pages / Vercel environment variables)
# If running locally with local backend, you can leave it empty or set to http://localhost:8000
VITE_API_URL=https://fxbrain-backend.onrender.com
